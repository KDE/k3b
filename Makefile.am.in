# these are here to control the compilation order, but are only textual
# hints for the cvs.sh script. If you change something here, you have
# to call Makefile.cvs again
COMPILE_BEFORE_libk3b = libk3bdevice
COMPILE_BEFORE_src = libk3b
COMPILE_BEFORE_plugins = libk3b
COMPILE_BEFORE_k3bsetup = libk3b

EXTRA_DIST = AUTHORS COPYING ChangeLog INSTALL README TODO k3b.lsm 

AUTOMAKE_OPTIONS = foreign 

