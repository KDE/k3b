if test x$ogg_vorbis = xno; then
	echo "----------------------------------------"
	echo "No Ogg Vorbis library and headers found."
	echo "Ogg Vorbis support disabled."
	echo "----------------------------------------"
fi

if test $mad_detect = 0 ; then
	echo "----------------------------------------"
	echo "No mad library and headers found."
	echo "K3b will not compile without them!"
	echo "----------------------------------------"
fi
