Install K3b 0.4.0

K3b 0.4.0 requires the ID3Lib which can be downloaded from id3lib.sourceforge.net.
So first you need to install (configure/make/make install) the id3lib.

Then install K3b like every other KDE app:

./configure

make

make install (as root)


Have fun
Sebastian Trueg
